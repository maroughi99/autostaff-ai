[build]
  command = "cd apps/web && npm install && npm run build"
  publish = "apps/web/.next"
  base = "."

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
